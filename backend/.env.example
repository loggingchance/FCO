FIA_API_BASE_URL=https://apps.fs.usda.gov/fiadb-api
FIA_DEFAULT_EVALUATION_YEAR=2023
FIA_TIMEOUT_SECONDS=30
# The API is public and defaults to all origins. Set a comma-separated list
# only if deployment policy later requires origin restrictions.
CORS_ORIGINS=*
